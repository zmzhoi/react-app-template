import FlexBox from '@/components/FlexBox';

function App() {
  /**
   * Remove code below
   */
  return (
    <FlexBox>
      <h1>{`<React App Template />`}</h1>
    </FlexBox>
  );
}

export default App;
